use anyhow::{bail, Result};

use crate::AocResult;

pub fn task11() -> Result<AocResult<i32, i32>> {
    let task = std::hint::black_box(include_str!("../tasks/task11.txt"));

    Ok(AocResult {
        a: 0,
        b: 0,
    })
}
